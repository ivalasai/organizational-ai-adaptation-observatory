[panel]
output_path = "data/panel/firm_year_panel.parquet"
export_formats = ["parquet", "csv"]
min_year = 2010

feature_tables = [
    "attention/sec",
    "attention/earnings_calls",
    "investment/patents",
    "investment/jobs",
    "deployment/products",
]

join_keys = ["firm_id", "year"]
