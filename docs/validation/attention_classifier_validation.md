# Attention Classifier Validation

Source labels: `data/validation/attention_labels.csv`

## Status

**Pending human labeling.** Fill the ``human_ai_mention`` column in the labels CSV
(1 = excerpt discusses AI/ML topics, 0 = does not), then run:

```bash
oaa validation run --labels data/validation/attention_labels.csv
```

## Metrics

| Metric | Value |
|--------|------:|
| Precision | _pending_ |
| Recall | _pending_ |
| F1 | _pending_ |

Do not cite this layer as validated until real numbers appear above.
